// Q.>> Problem  No.4 >>

function foo1() {
      return
      {
            bar: "hello"
      };
}
function foo2() {
return {
      bar: "hello"
};
}

// We can use the same code many times with different arguments, and it is produce different or same results.
