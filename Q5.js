// Q.>> Problem No.5 >>


var arr = [2, 56, 78, 34, 65];
var new_arr = arr.map(function (num) {
      return num / 2;
});
print(new_arr);

// [[currently open her page send to the  Printer for print it   ]]
